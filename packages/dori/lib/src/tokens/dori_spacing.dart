/// 📐 Dori Design System spacing tokens
///
/// Flat spacing scale based on multiples of 4dp.
/// Use these tokens to ensure visual consistency across the app.
///
/// ## Scale
/// - **xxxs**: 4dp — Micro spacing (between icon and text)
/// - **xxs**: 8dp — Between very close items
/// - **xs**: 16dp — Between list items
/// - **sm**: 24dp — Card padding
/// - **md**: 32dp — Between sections
/// - **lg**: 48dp — Page margins
/// - **xl**: 64dp — Large spaces, hero sections
///
/// {@category Tokens}
class DoriSpacing {
  const DoriSpacing._();

  /// 4dp — Micro spacing (between icon and text)
  static const double xxxs = 4;

  /// 8dp — Between very close items
  static const double xxs = 8;

  /// 16dp — Between list items
  static const double xs = 16;

  /// 24dp — Card padding
  static const double sm = 24;

  /// 32dp — Between sections
  static const double md = 32;

  /// 48dp — Page margins
  static const double lg = 48;

  /// 64dp — Large spaces, hero sections
  static const double xl = 64;
}
